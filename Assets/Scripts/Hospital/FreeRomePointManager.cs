using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Management.Hospital
{
    //This class is a manager of free rome points
    //When patient is having no place to go, Like doctor is busy and waiting lobby is full,
    //Then he will prefer to go back to home or rome inside the hospital
    //And if he finds the waiting lobby seat is empty then will go and seat there
    public class FreeRomePointManager : MonoBehaviour
    {
        
    }
}